import { ObservableInterface } from "@hibas123/utils";
import { Adapter, Message } from "./types.js";
export declare class ConsoleAdapter implements Adapter {
    private colors;
    constructor(colors?: boolean);
    init(observable: ObservableInterface<Message>): void;
    flush(): void;
    private formatLine;
    onMessage(message: Message): void;
}
